{
  "name": "hostech-fullstack",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix frontendV2Hostech",
    "dev:premium": "npm run dev --prefix frontend",
    "dev:v2": "npm run dev --prefix frontendV2Hostech",
    "dev:backend": "cd backend && php artisan serve",
    "dev:allv2": "npx concurrently \"npm run dev:v2\" \"npm run dev:backend\"",
    "dev:allv1": "npx concurrently \"npm run dev:premium\" \"npm run dev:backend\""
  },
  "dependencies": {
    "all": "^0.0.0"
  }
}
